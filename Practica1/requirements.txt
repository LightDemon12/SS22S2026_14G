# Requisitos para ejecutar el proceso ETL (Practica 1 - Seminario de Sistemas 2)
# Instalar con: pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
SQLAlchemy>=2.0

# Necesario SOLO si vas a conectar a SQL Server (entrega oficial).
# Requiere ademas tener instalado el driver ODBC de Microsoft en el sistema
# operativo (Microsoft ODBC Driver 17/18 for SQL Server).
pyodbc>=5.0

# Utilizado unicamente para regenerar el diagrama del modelo dimensional
# (diagrams/generar_diagrama.py). Requiere el binario "dot" de Graphviz
# instalado en el sistema (https://graphviz.org/download/).
graphviz>=0.20
